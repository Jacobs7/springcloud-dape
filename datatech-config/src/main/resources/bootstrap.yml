eureka:                        #eureka注册中心配置
  client:
    fetch-registry: true       #从注册中心更新
    register-with-eureka: true #注册到注册中心
    service-url:
      defaultZone: http://peer1:8801/eureka  #注册中心地址
  instance:
    hostname: peer1            #实例名称
server:
  port: 8888                   #服务端口
spring:
  cloud:
    config:
      server:
        git:                   #SpringCloudConfig 配置中心Git设置 消息总线回根据此处Git 配置更新配置文件
          uri: https://gitee.com/alive768/datatech-config
          searchPaths: springcloud-config
          username: xushifu@live.cn
          password: 198568xz
      label: master           #Git分支
  application:
    name: config-server       #实例名称