server:
  port: 8882
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      uri: http://peer1:8888
      profile: dev
management:
  endpoints:
    web:
      exposure:
        include: "*"